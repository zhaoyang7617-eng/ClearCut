name: Build Chinese APK

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - 'app/src/main/res/values-zh-rCN/**'
      - 'app/src/main/res/xml/locales_config.xml'
      - '.github/workflows/build-zhcn-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Make Gradle executable
        run: chmod +x gradlew
      - name: Build debug APK
        run: ./gradlew assembleDebug --stacktrace --dependency-verification=off
      - name: Collect arm64-v8a APK only
        if: success()
        run: |
          mkdir -p out
          APK="$(find app/build/outputs/apk -type f -name "*arm64-v8a*.apk" | head -n 1)"
          test -n "$APK"
          cp "$APK" out/ClearCut-v3.81.0-zhCN-arm64-v8a-debug.apk
          ls -lh out/
      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ClearCut-v3.81.0-zhCN-arm64-v8a
          path: out/*.apk
          if-no-files-found: error
